#!/bin/sh
psql -h dop50 -U postgres test <<_EOF_ > tC_tmp.psql_out 2>&1
\i ../../../test/ABC.sql
\q
_EOF_

# LOFAR_CHECKTOOL can define a command (like valgrind) for the executable.
$LOFAR_CHECKTOOL ./tC
