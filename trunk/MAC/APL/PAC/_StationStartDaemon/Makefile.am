SUBDIRS=src

DISTCHECK_CONFIGURE_FLAGS=\
      --with-common=$(prefix)

EXTRA_DIST = \
      Makefile.common \
      StationStartDaemon.spec \
      autoconf_share/compiletool

include $(top_srcdir)/Makefile.common
