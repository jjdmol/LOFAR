#!/bin/sh


PATH=/usr/local/cuda/bin:${PYTHONPATH}
export PATH
LD_LIBRARY_PATH=/usr/local/cuda/lib64
export LD_LIBRARY_PATH

python KernelPerformance.py

if [ $? -ne 0 ]; then
  exit 1
else
  exit 0
fi
