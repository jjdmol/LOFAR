README: pps-tune
================


Requirements
------------

  * ``/opt/stationtest/verify.py``
  * ``/opt/stationtest/modules/cli`` (``commands.getoutput`` wrapper)

  
Imports ``operator``, ``array``, ``ones``, ``math``, and ``commands``
are not used.


Installation
------------

This is a stand-alone script that resides in ``/opt/stationtest`` at
LOFAR stations. It calls the ``/opt/stationtest/verify.py`` script at
several places.
