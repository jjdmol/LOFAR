# The user with which to log into the storage cluster. Defaults to $USER.
Cobalt.OutputProc.userName = 

# The public & private keys used for logging in.
# By default, ~/.ssh/id_rsa (.pub) and ~/.ssh/id_dsa (.pub) are tried.
Cobalt.OutputProc.sshPublicKey =
Cobalt.OutputProc.sshPrivateKey =

# The executable of the process to start. Defaults to `outputProc',
# which thus needs to be found in $PATH after login.
Cobalt.OutputProc.executable =

