#!/bin/sh -x

# Check for GPU
#if ! lspci | grep -E "VGA|3D" | grep -E "ATI|NVIDIA"
#then
#  echo "No ATI/NVIDIA graphics cards detected on host `hostname`" \
#       "-- aborting test." >&2
#  exit 3
#fi

./tIntToFloatKernel

